import { SIZES } from "../../commonStyles";
import styled from "styled-components";

const H1 = styled.h1`
  font-size: ${SIZES.huge};
  margin: 0;
  padding: 1rem;
  text-align: center;
`;

export default H1;
